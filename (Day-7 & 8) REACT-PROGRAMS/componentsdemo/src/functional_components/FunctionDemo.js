import React from 'react';

function FunctionDemo(props) {
  return (
    <div>
      <h2>Hi, {props.username} working with {props.company}</h2>
      class
    </div>
  );
}

export default FunctionDemo;